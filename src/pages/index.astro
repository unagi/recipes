---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE, CATEGORIES } from '../consts';
---

<!doctype html>
<html lang="ja">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<h1>今日の献立、ここから探す</h1>
				<p class="subtitle">{SITE_DESCRIPTION}</p>
			</section>

			<section class="features">
				<div class="feature-card">
					<span class="material-icons feature-icon">rice_bowl</span>
					<h2>{CATEGORIES.japanese.label}</h2>
					<p>家庭で作れる定番和食から本格的な料理まで、和の味わいを楽しめるレシピ集。</p>
					<a href="/japanese/" class="button">レシピを見る</a>
				</div>
				<div class="feature-card">
					<span class="material-icons feature-icon">restaurant</span>
					<h2>{CATEGORIES.western.label}</h2>
					<p>洋風の定番料理やおもてなし料理など、西洋料理のレシピを紹介。</p>
					<a href="/western/" class="button">レシピを見る</a>
				</div>
				<div class="feature-card">
					<span class="material-icons feature-icon">ramen_dining</span>
					<h2>{CATEGORIES.chinese.label}</h2>
					<p>本格的な中華料理から家庭で作りやすいアレンジレシピまで。</p>
					<a href="/chinese/" class="button">レシピを見る</a>
				</div>
				<div class="feature-card">
					<span class="material-icons feature-icon">kebab_dining</span>
					<h2>{CATEGORIES.ethnic.label}</h2>
					<p>アジア、中東、南米などの各国料理で食卓に彩りを。</p>
					<a href="/ethnic/" class="button">レシピを見る</a>
				</div>
				<div class="feature-card">
					<span class="material-icons feature-icon">inventory_2</span>
					<h2>{CATEGORIES['preserved-sauces'].label}</h2>
					<p>作り置きできる保存食やタレで、毎日の料理をもっと便利に。</p>
					<a href="/preserved-sauces/" class="button">レシピを見る</a>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.hero {
		text-align: center;
		padding: 4rem 1rem 3rem;
		margin-bottom: 2rem;
	}
	.hero h1 {
		font-size: 3rem;
		margin-bottom: 1rem;
		line-height: 1.2;
		color: rgb(var(--black));
	}
	.subtitle {
		font-size: 1.25rem;
		color: rgb(var(--gray));
		max-width: 600px;
		margin: 0 auto;
	}
	.features {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 2rem;
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 1rem 2rem;
	}
	.feature-card {
		background: white;
		border-radius: 16px;
		box-shadow: var(--box-shadow);
		padding: 2.5rem 2rem;
		text-align: center;
		transition: transform 0.2s ease, box-shadow 0.3s ease;
		display: flex;
		flex-direction: column;
		align-items: center;
	}
	.feature-card:hover {
		transform: translateY(-4px);
		box-shadow: 0 4px 16px rgba(var(--gray), 25%), 0 12px 28px rgba(var(--gray), 30%);
	}
	.feature-icon {
		font-size: 64px !important;
		color: rgb(var(--accent));
		display: block;
		margin-bottom: 1rem;
	}
	.feature-card h2 {
		margin-bottom: 1rem;
		font-size: 1.75rem;
		color: rgb(var(--black));
	}
	.feature-card p {
		margin-bottom: 2rem;
		color: rgb(var(--gray));
		line-height: 1.6;
		flex-grow: 1;
	}
	.button {
		display: inline-block;
		background: rgb(var(--accent));
		color: white;
		padding: 0.875rem 2rem;
		border-radius: 12px;
		text-decoration: none;
		font-weight: 600;
		font-size: 1rem;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
		margin-top: auto;
	}
	.button:hover {
		transform: translateY(-2px);
		box-shadow: 0 6px 16px rgba(var(--accent), 0.4);
	}
	@media (max-width: 720px) {
		.hero h1 {
			font-size: 2rem;
		}
		.subtitle {
			font-size: 1rem;
		}
		.features {
			padding-bottom: 1rem;
		}
		.feature-icon {
			font-size: 48px !important;
		}
	}
</style>
