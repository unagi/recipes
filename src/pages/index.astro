---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="ja">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<h1>今日の献立、ここから探す</h1>
				<p class="subtitle">{SITE_DESCRIPTION}</p>
			</section>

			<section class="features">
				<div class="feature-card">
					<h2>📖 レシピ</h2>
					<p>さまざまな料理のレシピを公開しています。材料、手順、調理時間など詳しい情報が確認できます。</p>
					<a href="/recipes/" class="button">レシピ一覧を見る</a>
				</div>

				<div class="feature-card">
					<h2>📝 ブログ</h2>
					<p>料理に関する記事や、レシピ開発の裏話などを投稿しています。</p>
					<a href="/blog/" class="button">ブログを見る</a>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.hero {
		text-align: center;
		padding: 4rem 1rem 3rem;
		margin-bottom: 2rem;
	}
	.hero h1 {
		font-size: 3rem;
		margin-bottom: 1rem;
		line-height: 1.2;
	}
	.subtitle {
		font-size: 1.25rem;
		color: rgba(var(--gray), 1);
		max-width: 600px;
		margin: 0 auto;
	}
	.features {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		max-width: 800px;
		margin: 0 auto;
		padding: 0 1rem;
	}
	.feature-card {
		background: white;
		border-radius: 12px;
		box-shadow: var(--box-shadow);
		padding: 2rem;
		text-align: center;
	}
	.feature-card h2 {
		margin-bottom: 1rem;
		font-size: 1.5rem;
	}
	.feature-card p {
		margin-bottom: 1.5rem;
		color: rgba(var(--gray), 1);
		line-height: 1.6;
	}
	.button {
		display: inline-block;
		background: rgb(var(--accent));
		color: white;
		padding: 0.75rem 1.5rem;
		border-radius: 8px;
		text-decoration: none;
		font-weight: 600;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
	}
	.button:hover {
		transform: translateY(-2px);
		box-shadow: 0 4px 12px rgba(var(--accent), 0.3);
	}
	@media (max-width: 720px) {
		.hero h1 {
			font-size: 2rem;
		}
		.subtitle {
			font-size: 1rem;
		}
		.features {
			grid-template-columns: 1fr;
		}
	}
</style>
